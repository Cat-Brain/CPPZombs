#version 330 core
layout (location = 0) in vec2 aPos;

uniform mat4 perspective;
uniform vec3 position;
uniform float range;
out vec2 screenPos;

void main()
{
	gl_Position = perspective * vec4(position + vec3(aPos * range, 0.), 1.);
	screenPos = gl_Position.xy / gl_Position.w * 0.5 + 0.5;
} 