#version 330 core
layout (location = 0) in vec2 aPos;

uniform ivec2 center, scrDim;
uniform int range;
out vec2 pos;

void main()
{
	pos = aPos * 2 * range + center - range;
	gl_Position = vec4(pos / scrDim, 0.0, 1.0);
} 