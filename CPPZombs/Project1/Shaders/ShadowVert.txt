#version 330 core
layout (location = 0) in vec3 aPos;

uniform mat4 perspective;
uniform vec3 position;
uniform float range;

void main()
{
	gl_Position = perspective * vec4(position + aPos * range, 1.);
} 