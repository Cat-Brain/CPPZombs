#version 330 core
layout (location = 0) in vec2 aPos;

out vec2 shadUV, uv;
uniform vec2 screenDim, pos;
uniform float currentScrRat, newScrRat;
void main()
{
	gl_Position = vec4(aPos, 1.0, 1.0);
	uv = aPos / 2 + 0.5;
	shadUV = (uv * screenDim + pos) / screenDim;
} 