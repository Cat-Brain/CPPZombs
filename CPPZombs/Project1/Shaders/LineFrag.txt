#version 330 core
layout (location = 0) out vec4 FragColor;
layout (location = 1) out vec4 normal;
layout (location = 2) out vec4 position;

uniform vec4 color;

in vec2 dir;
in float dist;

void main()
{
	FragColor = color;

	normal = vec4(dir * dist, sqrt(1. - dist * dist), color.a);
} 