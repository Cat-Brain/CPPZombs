#version 330 core
layout (location = 0) in vec3 aPos;

uniform vec4 posScale;
uniform mat4 perspective;

out vec3 tPos;

void main()
{
	tPos = aPos * posScale.w + posScale.xyz;
	gl_Position = perspective * vec4(tPos, 1.0);
} 